{
  "name": "doclock",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix server",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix doclock-project/client"
  },
  "dependencies": {
    "axios": "^1.13.6"
  }
}
